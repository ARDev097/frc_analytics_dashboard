import type { Metadata, Viewport } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import { DataStoreProvider } from '@/lib/data-store'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'FRC Gujarat - School Fees Analytics Dashboard',
  description: 'Professional analytics dashboard for Gujarat school fee data. Compare fees, analyze market trends, and build evidence for FRC applications.',
  generator: 'v0.app',
  keywords: ['Gujarat', 'school fees', 'FRC', 'Fee Regulatory Commission', 'education', 'analytics'],
  authors: [{ name: 'FRC Gujarat Analytics' }],
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  themeColor: '#1a1a2e',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className="font-sans antialiased">
        <DataStoreProvider>
          {children}
          <Analytics />
        </DataStoreProvider>
      </body>
    </html>
  )
}
